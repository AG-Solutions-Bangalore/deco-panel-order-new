import { CreateOrderPage } from "@/modules/orders/pages/create-order-page";

export const metadata = {
  title: "Create Order | Deco Panel",
};

export default function Page() {
  return <CreateOrderPage />;
}
